基本调完也测试完，即将发布的状态。

目前来说，HTML5版在功能上来讲无法完全模拟Java版实现。一是因为支持WebGL的浏览器普及率问题（不过，因为没有用WebGL，所以它可以被绝大多数浏览器支持），二是出自JS文件的体积大小考虑，不可能把几MB的代码都移植过来，否则，哎……。所以，目前的HTML5版仅仅是LGame标准版的功能缩小实现（所有模块都有，但各个模块都只提供了主要功能），整理后7月分应该能发出来了。

